import { RouteData } from 'router/types';
import { IconName } from 'components/icon/types';

export type PageHeaderMainLinkProps = {
  routeData: RouteData;
};

export type LinkIconName = Partial<IconName>;
